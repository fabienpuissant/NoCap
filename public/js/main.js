function like()
{
    //S'effectue lorsque l'utilisateur clique sur un bouton like d'une photo

    //Récupération de l'id de la photo
    var photo_id = $('.vote[data-ref-id]');
    alert(photo_id);
}

