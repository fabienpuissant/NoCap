{% extends "base.html.twig" %}


{% block title %}Ajouter{% endblock %}

{% block body %}

    {% if err == 1 %}
        <div class = "alert alert-success">
			<p class = "alert">Utilisateur enregistré</p>
	    </div>
    {% endif %}

    {% if err == 2 %}
        <div class = "alert alert-danger">
			<p class = "alert">Email deja enregistré</p>
	    </div>
    {% endif %}

        <div class = "row mt-4 mr-4">
        <button class="btn btn-primary" style="margin-left:5vw"><a href="/admin/index" style="color:white;text-decoration:none;" />Retour</a></button>
        </div>


    <div class = "container mt-4">
        <div class = "row">
            <div style = "width: 75vw;" class = "justify-content-center">
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}